export default class Hospital {
    constructor(
        private id: number,
        private name: string,
        private cnpj: string,
        private phone: string,
        private address: string,
    ) {}
}
